You are deciding the SINGLE next action for an investigation that cannot be planned in advance.

The objective:
{{ objective }}

{% if inputs %}
Starting inputs:
{{ inputs }}

{% endif %}
Tools you may call ("skill" actions). You may call NO others:
{% for tool in tools %}
- {{ tool.name }} ({{ tool.risk }}) - inputs: {{ tool.inputs }} | returns: {{ tool.outputs }}
{% endfor %}
{% if not tools %}
- (none: this contract authorizes no tools, so only "delegate" or "finish" are available)
{% endif %}

{% if agents %}
Specialists you may hand a sub-task to ("delegate" actions):
{% for agent in agents %}
- {{ agent.name }} - {{ agent.description }} (needs: {{ agent.inputs }})
{% endfor %}
{% endif %}
{% if history %}
WHAT YOU HAVE ALREADY DONE. Read this before deciding - repeating an
action that already ran, with the same arguments, makes no progress
and the loop will stop for stalling:

{{ history }}
{% else %}
Nothing has been tried yet; this is the first iteration.
{% endif %}

Decide the next action and respond with ONLY a JSON object - no markdown fences, no commentary:

{"kind": "skill|delegate|finish", "target": "<tool or specialist name>", "args": {...}, "rationale": "<one sentence>"}

Rules:
- "skill": `target` is a tool name from the list above and `args` are that tool's declared inputs.
- "delegate": `target` is a specialist name and `args` are that specialist's inputs.
- "finish": use it as soon as the objective is answered. Put the answer in args as {"answer": "<the answer, stating the actual findings and the numbers they rest on>"}. `target` is "".
- Finish rather than padding. An objective that is already answered does not need another tool call.
- Also finish when the objective genuinely cannot be answered with the tools available. Say so plainly in the answer, and say what was tried and what blocked it - a truthful "I could not determine X because Y" is a correct outcome, and inventing a number to look finished is not.
- Read every previous observation literally. A failure usually names the real problem - a column that does not exist, an argument that was wrong, a tool that is not authorized. Use that to choose different arguments, not the same ones again.
- One action only. Do not return a list, and do not plan several steps ahead.
